services:
  dockerize-react-app:
    container_name: dockerize-react-app
    image: dockerize-react-app
    restart: unless-stopped
    environment:
      - REACT_APP_API_BASE_URL=production.barikoi.com
    ports:
      - "5000:80"
